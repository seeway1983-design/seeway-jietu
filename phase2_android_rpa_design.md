# 第 2 轮方案设计稿

## 目标

将第 1 轮“电脑微信小程序试跑”升级为一个可稳定批量执行的截图方案，核心目标是：

- 稳定完成 11 个城市的卫龙搜索结果截图
- 输出规范文件名和目录结构
- 支持人工登录，不绕过账号体系
- 支持半自动起步，逐步过渡到高成功率批量执行

## 第 1 轮结论

第 1 轮已验证出以下问题：

- 电脑微信小程序的搜索输入框难以稳定接收自动化键盘输入
- 自动化过程中容易跳回微信“小程序中心”或其它窗口
- 很难得到统一、干净、可复用的全屏截图
- 不适合依赖“桌面坐标点击 + 微信小程序”继续深挖

结论：

- 电脑微信不适合作为正式批量主链路
- 第 2 轮应转向“安卓模拟器 + RPA”或“Windows + RPA”

## 推荐主方案

推荐顺序：

1. 安卓模拟器 + Python / 图像识别
2. Windows + 影刀 RPA / UiBot
3. 电脑微信仅保留为人工兜底方案

优先推荐安卓模拟器的原因：

- 页面形态更接近真实移动端小程序
- 搜索、切地址、滚动和截图更符合目标场景
- 更容易做固定分辨率、固定窗口大小、固定截图区域
- 后续更容易扩展到多品牌、多城市

## 方案 A：安卓模拟器

### 建议环境

- macOS 或 Windows 宿主机
- 安卓模拟器：
  - 雷电模拟器
  - MuMu 模拟器
  - 夜神模拟器
- 微信手机版
- 朴朴小程序
- Python 3.10+

### 建议技术栈

- `adb`
- `uiautomator2` 或 `adb shell input`
- `opencv-python`
- `pillow`
- `pathlib`
- `json`
- `argparse`
- `loguru` 或 `logging`

### 执行链路

1. 人工登录微信
2. 人工打开朴朴小程序
3. 脚本控制模拟器前台
4. 脚本切城市或切收货地址
5. 脚本搜索“卫龙”
6. 脚本对当前可视区域截图
7. 如需要整页，则脚本滚动并拼接长图
8. 保存到标准目录

### 优点

- 可稳定控制分辨率
- 更容易识别控件和页面状态
- 可接入 `adb exec-out screencap`，截图质量稳定
- 更适合后续做整页拼接

### 风险

- 模拟器性能与兼容性有差异
- 微信和朴朴小程序版本变化后，识别规则需要维护
- 若地址切换强依赖地图或授权弹窗，需要单独处理

## 方案 B：Windows + RPA

### 建议工具

- 影刀 RPA
- UiBot
- Power Automate Desktop

### 适用场景

- 需要快速搭建非技术同学可操作流程
- 需要可视化节点编排
- 后续由运营同学维护

### 执行链路

1. 人工登录微信或模拟器
2. RPA 识别窗口和控件
3. 切城市 / 地址
4. 搜索“卫龙”
5. 截图保存
6. 记录成功 / 失败城市

### 优点

- 上手快
- 节点式流程更容易交接
- 更适合半自动业务流程

### 风险

- 图像识别对界面波动较敏感
- 复杂异常恢复不如代码方案灵活
- 整页长截图能力通常要额外拼装

## 推荐架构

### 第 2 轮目标形态

- 人工登录
- 脚本自动切城市或切地址
- 脚本自动搜索卫龙
- 脚本自动截图
- 脚本记录城市执行结果

### 模块拆分

1. `config/`
   - `cities.json`
   - `brands.json`
   - `device.json`
   - `selectors.json`

2. `runner/`
   - `device.py`
   - `navigator.py`
   - `search.py`
   - `capture.py`
   - `report.py`

3. `artifacts/`
   - `screenshots/`
   - `logs/`
   - `debug/`

### 建议新增配置

`device.json`

```json
{
  "platform": "android_emulator",
  "adb_serial": "<adb-serial>",
  "screen_width": 1080,
  "screen_height": 1920
}
```

`selectors.json`

```json
{
  "home_search_box": "template_home_search_box.png",
  "address_entry": "template_address_entry.png",
  "search_result_anchor": "template_search_result_anchor.png"
}
```

## 截图策略

### 方案 1：当前可视区域截图

适合：

- 先快速跑通 11 城
- 品牌方接受“结果页首屏图”

优点：

- 实现最快
- 成功率最高

### 方案 2：整页长截图

适合：

- 需要完整商品列表
- 需要更规范的归档

实现方式：

1. 识别结果页顶部锚点
2. 连续下滑
3. 每次截图
4. 通过图像重叠区域拼接
5. 输出长图

风险：

- 列表异步加载时易错位
- 懒加载和浮层会影响拼接

建议：

- 第 2 轮先完成“当前可视区域稳定图”
- 第 3 轮再做整页长图拼接

## 文件命名建议

沿用当前项目命名规则，并增加执行状态：

```text
screenshots/
  2026-xx-xx/
    佛山市/
      2026-xx-xx_佛山市_卫龙_搜索列表.png
      2026-xx-xx_佛山市_卫龙_搜索列表_long.png
```

失败记录建议输出：

```text
logs/
  run_2026-xx-xx.log
  failed_2026-xx-xx.json
```

## 第 2 轮里程碑

### M2-1

- 确定执行环境
- 在模拟器或 Windows RPA 中手工跑通 1 个城市

### M2-2

- 跑通 11 个城市的“卫龙首屏截图”
- 记录每个城市所用地址

### M2-3

- 加入失败重试
- 输出执行报告

### M2-4

- 评估是否值得做整页长截图

## 我建议的落地顺序

1. 放弃继续深挖电脑微信自动输入
2. 选定一个安卓模拟器
3. 先只跑 1 个城市
4. 再扩到 11 个城市
5. 首先交付“首屏稳定截图”
6. 最后再评估整页拼接

## 对当前项目的建议

当前项目建议保留为：

- 第 1 轮实验记录
- 配置文件样例
- 命名规范参考

不要继续把 `run_mvp.py` 当作正式生产主干扩展。

第 2 轮建议新建独立目录，例如：

```text
pupu_price_phase2/
  config/
  logs/
  screenshots/
  templates/
  scripts/
  README.md
```

## 最终建议

如果目标是“给品牌方稳定出图”，推荐路线是：

- 第 2 轮：安卓模拟器首屏稳定截图
- 第 3 轮：长图拼接和更多品牌扩展

如果目标是“尽快由非技术同学使用”，推荐路线是：

- Windows + 影刀 RPA / UiBot
